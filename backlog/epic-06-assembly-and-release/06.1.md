### 06.1 — `CLAUDE.md` template rewrite

**What & why.** Fold in every operating-doc lesson from Mompa's own `CLAUDE.md` evolution, merged
with — not replacing — the existing §7 token-discipline section from v1.0.2.

**This increment.**
- **§0 — nested `CLAUDE.md` precedence** for multi-app workspaces: the app-level file wins for
  that app's internals, the root wins for cross-app strategy, conflicts get *surfaced* rather than
  silently resolved, reusable patterns *flow up*. Shipped as a conditional section (only relevant
  once a project has more than one app).
- **House standards as a lessons ledger** — the *pattern* and the instruction ("a standard earns
  its place by citing the incident that bought it"), not any of Mompa's own entries.
- **Supersession/cleanup rule** — a story that supersedes another closes it in the same pass, with
  both named failure modes (closing by assumption; closing by omission) and a
  `#### 🧹 Cleanup on close` block that `tech-qa` walks before merging.
- **Repo-only memory** + the full surface-routing table (status / plan / shortcuts / blockers /
  decisions / product / operating rules / narrative), each to its committed home — per the
  repo-only decision, no row points at the harness memory store.
- **Session-close traceability checklist** and **self-serve before declaring blocked** (only a
  semantics call genuinely needs a human; a vendor's data shape usually doesn't).
- The full delivery contract (branch-per-story, single PR, tech-qa-only-merges,
  planning-only-to-main exception) stated once here; every gate's own `SKILL.md` states only the
  *procedure* it executes, cross-referencing this section rather than restating the contract.
- **Fix C8** — states the canonical wrap-up sequence exactly once:
  `product-check → tech-planning → tech-build → PR → tech-qa → merge → record-learnings`, with
  `handoff` explicitly marked off that spine (opt-in, not routine). Every skill template links to
  this section instead of restating the order in its own words.

**Acceptance criteria.**
- The delivery contract appears in full in exactly one place (`CLAUDE.md`); every gate template
  that used to restate it now links instead.
- The wrap-up sequence is stated as one unambiguous chain, with `handoff`'s opt-in status called
  out explicitly.
- No routing-table row in the memory section points at an external memory store.

#### Closed: 06.1 — 2026-08-14

Built directly. `CLAUDE.md`'s template grew from a flat §1–§8 into §0 (new, nested-precedence,
conditional on multi-app) plus §1–§6 preserved and §7 expanded into eleven numbered subsections
(§7.1 lifecycle through §7.11 token discipline, kept verbatim from v1.0.2 per the story's own "merged
with — not replacing" instruction). §7.2 is the delivery contract's one canonical statement
(branch-per-story, single PR, tech-qa-only-merges, the planning-only-to-`main` exception); §7.3 is
Fix C8's wrap-up chain, `handoff` explicitly named off-spine; §7.7 is the repo-only memory
surface-routing table.

**Every gate template that restated the contract now links instead**, verified by direct read, not
assumed from the edit: `tech-planning`'s "Delivering the plan" section dropped its own rationale for
why one branch exists (now `CLAUDE.md` §7.2), keeping only its own concrete git-command procedure;
`tech-build`'s branch-binding step and deliver step did the same; `tech-qa`'s merge-authority
paragraph gained a link while keeping its own genuinely-unique procedural content (the FAIL-discipline
rules, the "don't chain into tech-build" rule) untouched, since that's `tech-qa`'s own behavior, not a
restatement of the general contract. `record-learnings`' own wrap-up-sequence mention was also
brought in line — it previously stated a shorter, inconsistent chain (`build → push + PR → review →
tech-qa → merge → record-learnings`, missing `product-check`/`tech-planning` and inventing a "review"
step neither gate names) — now it links to §7.3 instead of carrying its own, drifted copy.

**A real, pre-existing bug found and fixed as a side effect of this verification, not part of the
story's own scope but directly adjacent to it.** `qcode-charter`'s "Resolve the seam gap" section
instructs grepping `.claude/skills/` for the literal string `(to define: the project's seam
invariants` to find and resolve the seam-gap identically across `tech-planning`, `tech-build`, and
`tech-qa`. Checking this while verifying 06.1's own link additions found `tech-qa`'s actual wording
never matched that grep at all — it said "**Seams sealed** — *(to define: the project's seam checks,
from `architecture/`.)*", a different label and a different opening phrase, dating back to 03.3.
`tech-planning`'s own label also differed ("The seams hold" vs `tech-build`'s "Seams stay sealed"),
dating back to 03.1. A real `qcode-charter` pass following its own documented instructions would have
silently resolved two of the three and left `tech-qa`'s gap both unresolved and undetected — exactly
the "two readers disagree" failure class this whole framework exists to prevent. Standardized all
three (and confirmed `qcode-charter`'s own documented example already matched the now-common form) —
`grep -rn "(to define: the project's seam invariants"` now returns exactly three real occurrences
plus `qcode-charter`'s own two meta-mentions, nothing missed.

**Verified against a real `generate` run, not just structural inspection.** Every one of the 17
tokens used in the new template resolved with no leftovers on a fresh scaffold (`grep -o
'{{[A-Z_]*}}'` on the rendered file: none found); the eighteen `##`/`###` headings rendered in the
exact designed order (§0 through §8, §7.1–§7.11); `check-links` against that same scaffold reports
only the two already-known, already-tracked `skills-lock.json` gaps (06.2's own promise) — nothing
new. `check-links` against this repo's own real tree (with the 05.4 embedded-template-source
exclusion active): 0 dead links across 35 files. Full suite: 71/71.
