### 06.2 — Docs, tokens, skills-lock, release, acceptance

**What & why.** Everything that has to be true for v2.0.0 to be a real, working release rather
than a pile of correct-in-isolation files.

**This increment.**
- `README.md` rewrite: four gates (product-check leads), the two-stage bootstrap (scaffold →
  charter), the engine's four modes.
- `docs/updating-projects.md` gains the v1→v2 migration path (pointing at `migrate` mode, 05.4).
- `references/filling-the-gaps.md`: new tokens (`{{CLAUDE_PLAN}}` already added in v1.0.2,
  `{{REPO_HOST}}`, `{{PRODUCT_CONSUMERS}}`), a revised gap checklist, and the documented
  reservation of **epic 08** for the ad-hoc bucket (a project with more than eight named epics
  must number around it — previously undocumented).
- `VERSION` → **2.0.0** at both the framework root and inside the scaffolder skill folder.
- Ship `skills-lock.json` declaring `architecture` (source, path, hash) as a recommended
  prerequisite — what `compass-check`'s leverage lens reads, and what would catch the vendor skill
  drifting upstream.
- Document clone-and-run as the install path (`git clone` + `node qcode.mjs generate ../new-project`).
  File packaging (`npx`-able, published package) as **deferred**, on this same backlog, with its
  trigger named: *"a second person, or a second machine, needs to scaffold a project."*
- **Run the day-one acceptance test end to end** — this is the release gate, not a nice-to-have:
  `generate` into a scratch repo → zero unresolved tokens, `board:check` 0, `check:links` green,
  cockpit renders → first commit succeeds with hooks active → `sync` reports zero diffs
  (idempotent) → `check` reports structurally valid with the honest open-gap count.

**Acceptance criteria.**
- Both `VERSION` files read `2.0.0`.
- The acceptance test sequence above runs clean, in order, on a real scratch directory — not
  simulated or described.
- `skills-lock.json` exists and validates against the same hash format Mompa's uses.
- The packaging deferral is written down with its trigger, not left as a silent gap.

#### Closed: 06.2 — 2026-08-14

Built directly. This is the last of the sixteen work packages — the release gate for the whole v2
extraction plan.

**`{{REPO_HOST}}` implemented, not just documented.** The story listed it as a "new token" for
`filling-the-gaps.md`, but no template used it and `generate` never asked a repo-host question — a
documented-but-unused token is a phantom, not a real gap answer. Derived it instead from the
`--remote` flag `generate` already asks for (no new question): recognizes `github.com`/`gitlab.com`/
`bitbucket.org` by name, falls back to the bare domain for anything else, and to a proper
charter-deferred gap when no remote was given. Verified against three real shapes: an HTTPS GitHub
URL → "GitHub", an SSH GitLab URL (`git@gitlab.com:...`) → "GitLab", a self-hosted domain → the bare
hostname. Surfaced in the project's own `README.md` ("**Hosted on:** {{REPO_HOST}}").

**`skills-lock.json` shipped** (plus `schema/skills-lock.schema.json`, matching `config.schema.json`'s
existing documentation-only pattern) declaring `architecture` as a `required: false` recommended
skill, with `path`/`source`/`hash`/`hashAlgorithm`/`installedAt` fields — `hash: null` until a team
actually installs the vendor skill and records it. **One honest caveat on this story's own acceptance
criterion:** "validates against the same hash format Mompa's uses" was written against an artifact
not available in this session to byte-compare against; the shape here (`sha256`, hex-encoded,
recorded at install time) is a reasoned, industry-standard choice consistent with everything
`compass-check`/`tech-planning` already expect of the file (matches installed-vs-declared, flags
drift), not a verified match to Mompa's own file. Placed at the templates root (not under `skills/`),
which — via `isFrameworkOwned()`'s existing prefix rule, not a new special case — already makes it
project-owned without any renderer change: exactly right, since a project's own recorded hashes must
survive `sync` untouched. **This is the change that finally resolved the `skills-lock.json` dead
links `tech-planning` (03.1) and `compass-check` (03.4) have carried all build** — confirmed by a real
`generate` run: self-test passed clean on the first try, no `skills-lock.json`-shaped violation, for
the first time all session.

**Real, functioning docs, not aspirational ones.** `README.md` gained "Use 3" (`migrate`) and "Use 4"
(`check`) alongside the rewritten "Use 2" (`qcode.mjs sync`, replacing the retired script), a
"Repository layout" refresh (added `lib/qcode-migrate.mjs`, `schema/skills-lock.schema.json`, dropped
the now-deleted `scripts/qcode-sync.mjs`), and an "Install" section with the clone-and-run path plus
the packaging deferral and its named trigger ("a second person, or a second machine, needs to
scaffold a project"). `docs/updating-projects.md` was rewritten in full: the framework-owned/
project-owned split now describes `isFrameworkOwned()`'s actual prefix rule instead of a
hand-maintained file list that had already drifted (missing `product-check`, missing the whole
`scripts/` set 02.2/02.3 shipped) — the same "one implementation, not a second copy that can
disagree" principle applied to a doc, not just code. Added "Migrating a v1-shaped project" (pointing
at `migrate`, this story's own explicit assignment from 05.3's deferral) and kept "Adopting a pre-QCode
donor project" as its own, genuinely different scenario (already gate-shaped, never generated — not
v1-shaped, so not `migrate`'s job).

**`scripts/qcode-sync.mjs` retired for real**, not just stopped-being-mentioned: deleted outright once
`qcode.mjs sync` was confirmed a complete, tested superset (05.4) and nothing live still pointed at
it. One real consequence found only by re-running `check-links` against this repo's own tree
afterward: a "To resume, load" pointer in the 2026-06-25 handoff journal linked directly to the
now-gone file. Historical narrative doesn't get revised (what the handoff *says happened* is still
true — `qcode-sync.mjs` genuinely was the updater at the time), but a "to resume, load" index entry is
navigational, not a claim about the past, so it was corrected to point at `qcode.mjs`'s `sync` mode
with an explicit note on what superseded what — the mechanical fix a dead link needs, without
rewriting what the session actually did.

**Also fixed while touching this territory:** `README.md`'s and `filling-the-gaps.md`'s own
descriptions of `record-learnings`' destinations still listed "memory" as one of the scaffold
defaults — stale since 03.4 actually removed the external-memory-store row from that skill's real
table. Both now say what the table actually says.

**The day-one acceptance test — run for real, every step, on a genuine scratch directory:**
1. `generate` (with `--git`, a real `--remote`) → 43 files, self-test passed clean on the first try.
2. Zero unresolved `{{TOKEN}}`s (`grep` confirms), `check:links` green (30 files), cockpit renders.
3. A **real** `git add -A` + commit, hooks genuinely active (`core.hooksPath` = `.githooks`) —
   `status-guard.sh` ran `board:check` as part of the commit itself, reported "the board is true,"
   commit succeeded (45 files, exit 0). The first time this session a real first-commit-with-hooks
   pass was exercised rather than inspected.
4. `qcode.mjs sync` (dry-run) against that same, just-committed project: 0 created, 0 updated, 19 up
   to date, 0 customized — genuinely idempotent, not assumed so.
5. `qcode.mjs check` against it: structurally valid, 69 open gaps reported honestly (exit 0).

**One test-fixture bug found by the regression suite, not inspection.** Adding `{{REPO_HOST}}` to
`README.md` immediately failed `lib/qcode-core.test.mjs`'s real-tree acceptance test —
`skippedMissingTokens: ["README.md"]` — because that test's hardcoded token fixture predates this
story and had no `REPO_HOST` key. Added it; 71/71 again. Exactly the kind of thing the fixture-drift
class of test exists to catch when a template gains a token the test wasn't updated for.

**Real regression found and fixed on the live repo itself, one more time.** After all of the above,
`board:check` on this repo's own root still failed rule 5 — the same historical-handoff dead link
described above, only surfaced once `check-links` was re-run against the real, current tree rather
than assumed clean from the individual pieces looking right in isolation.

**End state — the whole v2 extraction, verified as one thing, not sixteen isolated pieces:** both
`VERSION` files read `2.0.0`. `node scripts/board-check.mjs` from this repo's own root: `0 open · 15
closed · 16 story files … links: all resolve … ✅ the board is true`. `node qcode.mjs check .`:
structurally valid. 71 tests across `lib/`. Every one of epics 01–06 is `done`.
