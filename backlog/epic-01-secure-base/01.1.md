### 01.1 — Commit token discipline (v1.0.2), branch, bootstrap the board

**What & why.** Two things needed to happen before any v2 work starts: the uncommitted
token/context-discipline diff (six files, `{{CLAUDE_PLAN}}` + CLAUDE.md §7) had to land on `main`
as its own patch release, and QCode-Method needed its own `PROJECT-STATUS.md`/`backlog/` so this
whole build could be tracked the way the method itself prescribes.

**Value archetype.** Enabler — no direct value; every later story depends on the board this
creates and the branch it runs on.

**This increment.**
- Bump `VERSION` and the scaffolder's `VERSION` to `1.0.2`.
- Commit the six-file token-discipline diff to `main` as `v1.0.2`.
- Create the `v2-extraction` branch off updated `main`.
- Write QCode-Method's own `backlog/00-roadmap.md`, six epic files (this one plus 02–06),
  `PROJECT-STATUS.md`, `TECH-DEBT.md`, `OPEN-QUESTIONS.md` at the framework root — in the **v1
  board shape** (flat epic files, Active increments + Recently done), deliberately, since the v2
  engine that would produce the ADR-059 shape doesn't exist yet.

**Path to target.** This repo's own board becomes `migrate` mode's first test subject at 05.4,
once the v2 engine exists — proving the v1→v2 path here, on a repo fully controlled by the person
building it, before it's ever run against Finosonido.

**Debt incurred.** None — this is the intended shape per the plan's own decision (dogfood: yes).

**Acceptance criteria.**
- `git log` on `main` shows the `v1.0.2` commit; both `VERSION` files read `1.0.2`.
- `v2-extraction` branch exists, checked out.
- `backlog/00-roadmap.md` + 6 epic files + `PROJECT-STATUS.md` + `TECH-DEBT.md` +
  `OPEN-QUESTIONS.md` exist at the framework root, cross-linking correctly.
- Every one of the 16 work packages from the plan has exactly one story home across epics 02–06.

#### Closed: 01.1 — 2026-08-13

Done as part of this same bootstrap pass — see the commit that adds these files.
